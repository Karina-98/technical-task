import { DivContainerBackground } from "./Container.styled"

export const ContainerBackground = ({children}) => {
    return <DivContainerBackground>
        {children}
        
    </DivContainerBackground>
    
}